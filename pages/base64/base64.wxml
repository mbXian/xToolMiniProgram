<view class="content">
  <view>
    <input class="weui-input" auto-focus placeholder="请输入要加密的内容" bindinput="bindInput"/>
  </view>
  <button type="primary" bindtap="handleEncode">加密</button>
  <text class="font-break" hidden="{{encodeData === '' || originalData === ''}}">加密结果是：{{encodeData}}</text>

</view>